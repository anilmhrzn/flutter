//Todo: make whole file from scratch and make a container and make use of box decoration to use Linear gradient make use of begin and end  enc
import 'package:flutter/material.dart';
import 'gradient_container_with_text.dart';
void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      backgroundColor: Colors.yellow,
      // floatingActionButton: ,

      body: MyGradientContainerWithText(),

    ),
  ));
}

